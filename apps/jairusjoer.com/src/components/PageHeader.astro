---
import { page } from '@config';
import { Image } from 'astro:assets';
---

{
  page.image && (
    <Image
      class="mb-6 size-12"
      src={await page.image()}
      alt=""
      width={144}
      height={144}
    />
  )
}

<p class="text-heading">{page.title}</p>
<p>{page.description}</p>
